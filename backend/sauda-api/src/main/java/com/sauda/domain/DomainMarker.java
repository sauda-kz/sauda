package com.sauda.domain;

/** Marker for domain model (JPA entities and enums).
 *
 * <p>When adding or changing entities / Flyway migrations, update the ER diagram in
 * {@code docs/er-diagram.md} in the same PR.
 */
public interface DomainMarker {}
